import type { Theme } from "@emotion/react";

const theme: Theme = {
  colors: {
    title: "#EDAC00",
    primary: "#000000",
    secondary: "#ECECEC",
    white: "#FFFFFF",
    block: "#B8B8B8",
  },
};

export default theme;
