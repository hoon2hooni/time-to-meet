import type { Theme } from "@emotion/react";

const theme: Theme = {
  colors: {
    title: "#EDAC00",
    primary: "#000000",
    secondary: "#F5F5F5",
    white: "#FFFFFF",
  },
};

export default theme;
